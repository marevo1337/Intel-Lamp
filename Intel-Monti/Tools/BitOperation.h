#pragma once

#include "Bool.h"

// Function to check the parity of the bits in a number
BOOL is_bits_even(char number);

// Function to check if the Auxiliary Carry (AC) flag is set
BOOL is_auxiliary_carry_set(int result);